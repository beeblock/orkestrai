export const SHOT_DIRECTION_SCHEMA = { type: 'object', additionalProperties: false, properties: { framing: { enum: ['custom', 'wide', 'full', 'medium', 'close_up', 'detail'] }, angle: { enum: ['eye_level', 'low', 'high', 'overhead'] }, motion: { enum: ['none', 'static', 'push_in', 'pull_out', 'pan_left', 'pan_right', 'orbit', 'tracking'] }, pace: { enum: ['gentle', 'normal', 'energetic'] } } };
export const VIDEO_CONFIG_SCHEMA = {
  type: 'object', additionalProperties: false, properties: {
    provider: { type: 'string', enum: ['fal', 'byteplus', 'higgsfield'], description: 'Explicit video provider, defaults to fal for existing workflows. Must match profileId. Never silently change provider or model.' },
    modelId: { type: 'string', maxLength: 240, description: 'Exact endpoint/model ID from video_workflow_models for the selected provider; legacy wan-2.7-text and kling-v3-pro-image remain supported on fal.' },
    parameters: { type: 'object', description: 'Provider input following the selected model contract, including nested arrays/objects. No credentials or inline base64. prompt may instead be configured in the top-level prompt field.' },
    mediaBindings: { type: 'array', maxItems: 50, items: { type: 'object', additionalProperties: false, properties: { pointer: { type: 'string', description: 'JSON pointer into parameters, e.g. /image_urls/0 or /elements/0/frontal_image_url.' }, nodeId: { type: 'string', format: 'uuid' }, path: { type: 'string', description: 'Workspace-relative image, audio or video path. Supply exactly one of nodeId and path.' } }, required: ['pointer'] } },
    characterBindings: { type: 'array', maxItems: 8, items: { type: 'object', additionalProperties: false, properties: { id: { type: 'string', format: 'uuid', description: 'Exact owner-locked character version ID from video_workflow_characters.' }, alias: { type: 'string', pattern: '^[a-zA-Z][a-zA-Z0-9_]*$', maxLength: 64, description: 'Unique persistent reference alias. Mention it as @{alias} in the prompt; unknown aliases block generation.' }, imagePointers: { type: 'array', minItems: 1, maxItems: 12, items: { type: 'string' }, description: 'One exact model input path for each locked character image, in order.' }, voicePointer: { type: 'string', description: 'Exact audio-reference or compatible provider voice-ID input path. The server injects the locked identity.' } }, required: ['id', 'imagePointers', 'voicePointer'] } },
    billingUnits: { type: ['number', 'null'], exclusiveMinimum: 0, maximum: 1000000000, description: 'Explicit estimated billing quantity for units not derivable from the model input. Does not cap the provider invoice.' },
    profileId: { type: ['string', 'null'], format: 'uuid' },
    requiredCharacterIds: { type: 'array', maxItems: 8, uniqueItems: true, items: { type: 'string', format: 'uuid' } },
    requiredReferenceNodeIds: { type: 'array', maxItems: 50, uniqueItems: true, items: { type: 'string', format: 'uuid' } },
    prompt: { type: 'string', maxLength: 50000 }, negativePrompt: { type: 'string', maxLength: 2500 },
    duration: { type: 'number', minimum: 1, maximum: 120 }, shot: SHOT_DIRECTION_SCHEMA,
    aspectRatio: { type: 'string', enum: ['16:9', '9:16', '1:1', '4:3', '3:4'] },
    resolution: { type: 'string', enum: ['720p', '1080p'] }, generateAudio: { type: 'boolean' },
    seed: { type: ['integer', 'null'], minimum: 0, maximum: 2147483647 },
    startImageNodeId: { type: ['string', 'null'], format: 'uuid' }, endImageNodeId: { type: ['string', 'null'], format: 'uuid' },
    contextNodeIds: { type: 'array', maxItems: 8, uniqueItems: true, items: { type: 'string', format: 'uuid' } },
    outputDirectory: { type: 'string', maxLength: 500 }, filePrefix: { type: 'string', maxLength: 80 },
  },
};
export const VIDEO_TOOL_DESCRIPTIONS = {
  import: 'Add an existing finished or edited project video to the Canvas as a native playable Video node, without generation, provider credentials or paid opt-in. Use input.path relative to the configured workspace project folder, input.title and optional expectedSha256. Validates the file and keeps its original path and bytes (up to 256 MiB); never moves, deletes, transcodes or uploads it. Returns node, reused and workspace root; repeated imports of the same current path/floor reuse the existing node. The agent node supplies the floor and connection. Use for renders/final.mp4 and other local montages, not the owner-only multipart upload route or desktop automation. Do not restore previously removed media unless the user explicitly requests it.',
  sequences: 'Native non-destructive video assembly. list/read/create/apply/export/cancel/remove use the same Canvas/Workbench sequence. Add current-workspace MP4/WebM/MKV Video nodes; the service probes and hashes sources. apply operations add/update(in,out,volume,caption)/move/remove/settings require the current revision; settings dimensions are even pixels 240..1920, fps 24/25/30. At most 30 clips and 600 seconds. Captions burn in within title-safe margins; fit preserves all content with letterboxing, never silent crop/stretch. The owner installs the verified local encoder from the sequence UI; agents cannot install executables. export requires revision and a stable UUID idempotencyKey, returns actual background export state, and creates a new native Video node on success. Poll read; running/failed is not delivered. Cancel stops encoding without changing sources. Export is local, never a new fal generation. Transfer a sequence with all its source video nodes. Never use ffmpeg to modify Codex image outputs.',
  recipes: 'Capture versioned reusable briefs from a storyboard or group containing exactly one storyboard. Supply the current storyboard revision. Recipes include ordered scene direction, dialogue, duration, shot and named local reference/character slots, never run IDs, sessions, credentials or paid grants. Instantiate with explicit current-workspace input IDs, script and aspectRatio to create an editable native storyboard; it never auto-generates. Agents must preserve the exact approved character identity/version/digest. Only owners can reuse across workspaces or deliberately choose a different approved character. queue reads actual image/video workflow states, reservation amounts, errors and outputs. Open the linked workflow to estimate/run or repair inputs; use existing cancel/retry_download tools. Never resubmit submission_uncertain work or treat partial outputs as completed delivery.',
  brands: 'Versioned workspace brand kits: name, palette, logos/products/style images, tone and rules. Create/update drafts with revision guards. Only the owner can approve/lock. Fork an approved version to edit without changing it. Place a locked kit as connected native notes/images/group, with exact frozen references and fingerprints. Agents can only access kits in their current workspace; cross-workspace library/import is owner-only. Connect the resulting note and relevant reference images to existing native workflows; never duplicate credentials or grants, silently replace a locked kit or bypass ImageGen/reference limits.',
  assets: 'List image/video variants and inspect one exact output, its file hash, originating run, character versions and review history. Owner approvals bind to the inspected digest and become stale when bytes or provenance change. Agents can submit decision=proposed with the inspected expectedDigest, current review revision (0 if absent), and a comment; only the human owner can approve/reject/request changes. Inspection is not visual verification: inspect the actual image/video and sound before proposing. Never claim a generated result is approved because the job completed. Use prepare with expectedDigest and edit.operation=variation|remove_background|annotated_change|animate to create a connected draft from an image. Image edits use existing Codex ImageGen only; annotate a normalized rectangle with verified sourceWidth/sourceHeight, not a local raster mask. The original stays unchanged and the exact source is frozen. Inspect and run the returned workflow separately under normal permissions. No preparation call generates or charges.',
  storyboards: 'Native Canvas/Workbench storyboard. List/read/create, then apply bounded operations with the exact current revision. Each scene has direction, dialogue, language, requested duration, owner-locked character version IDs, reference image/video node IDs, optional Codex executor and linked image/video workflows. Materialize creates connected drafts only, never generates or charges. Save first, materialize each scene, inspect/open the resulting workflow, bind all required identities and references, then preview and run through existing controls. Reference images still use Codex ImageGen only. Changed scenes mark linked drafts stale; originals remain intact. Duplicate scenes clear workflow links. Deleting a scene or board never deletes output files. Unavailable copied references require explicit local repair. Agents cannot replace existing locked character selections or approve identities.',
  characters: 'Workspace Character Library. List/read saved versions; create/update drafts; fork an approved version without changing it. Only the owner can lock a version after reviewing appearance and voice. Locked records and their hashed local reference copies are immutable; agents cannot delete, overwrite, approve or silently replace them. Voice identity must be an approved audio reference or a provider voice ID scoped to account and exact compatible model IDs, never only a text description. Use command=binding with id and config to obtain the same conservative automatic input mapping as the UI; unsupported mappings return an explicit error and never drop references. Optional unique aliases in characterBindings can be mentioned as @{alias} in the prompt; unknown or removed aliases block generation. Bind locked IDs with config.characterBindings for every scene. Put approval history, source/model names and internal comments in definition.productionNotes, excluded from generation prompts. Appearance and voice.style are model-visible casting directions, not provenance or dialogue. Write exact dialogue in the scene and inspect the effective preview before paying. Fork locked versions for corrections. This locks inputs, not a guarantee of generative output consistency; review each delivered clip visually and audibly.',
  models: 'Discover video endpoints with input.provider (fal, byteplus or higgsfield) and query/offset/limit. fal uses live discovery; BytePlus and Higgsfield use reviewed versioned contracts. Never assume equal model names have equal inputs or pricing. With input.endpoint, return the normalized current input/output schema, descriptions, examples, defaults, constraints, documentation URL and digest. Examples are suggestions, not exclusive enums. Read the descriptions before create/update: prompt expansion rewrites direction, it is not render speed. Never enable rewriting without the owner agreeing. Unsupported contracts fail explicitly. With input.pricingIds (up to 50) and input.profileId instead of endpoint, read available base rates and billing units for permitted models, without generating, reserving or exposing credentials. Rates can be cached for five minutes and are NOT final quotes; audio/resolution multipliers may apply. Use preview for the configured run. Select exact IDs, not guessed names. Deprecated endpoints cannot generate. Configure parameters and mediaBindings. Availability in the catalog is not account authorization.',
  list: 'Discover the configured workspace project root, native video workflows, allowed account IDs, model capabilities and current image/video/note node IDs. All relative output paths resolve under workspace.workingDir (host path); wslWorkingDir is its Linux counterpart. A terminal cd never changes the authenticated workspace. No credentials are returned.',
  read: 'Read the workflow revision, config, run statuses and outputs still on Canvas. Deleted media is excluded even when files survive on disk. input.includeHistory=true reveals historical snapshots/outputs only when the user explicitly asks; history is not permission to reuse removed assets. queued is not completed. Poll at most once every 15 seconds while running.',
  create: 'Create a native videoWorkflow node without generating or charging. Plan scene prompts, dialogue, language and sound direction in Notes. Generate storyboard/reference images with the existing Codex image_workflow tools and image_gen.imagegen, never external-provider image generation. Wait for validated Image outputs, then bind their exact node IDs with mediaBindings. Discover video models and read their exact contract first using video_workflow_models. If speech or sound is required, select a model that documents native audio and the requested language; enable its documented audio parameter and include dialogue/sound direction in the prompt. Do not assume all video models speak, silently substitute a mute model, or promise identical voices between clips. Use allowed profiles from list. Uploading approved references for video is not generating images on fal. Set config.provider explicitly and choose a matching profile; changing provider must be owner-approved and preserve all required character/reference bindings for explicit remapping. No API key belongs in input.',
  update: 'Replace video workflow title/config using its current revision, without starting generation. For an unsaved draft returned by list/read, omit revision on the first save; persisted workflows require their exact revision. Explicit note/image IDs bind inputs; ordinary agent communication edges do not execute branches.',
  preview: 'Validate current references and estimate the selected provider cost without generating. Higgsfield uploads approved references to quote the exact configured input through the account; BytePlus uses dated public list rates and conservative input-video duration. Return preview ID/revision, estimate, reservation and price provenance. No estimate or reservation guarantees the final invoice. Owner policy and outbound-data/paid-operation gates apply. BytePlus requires public video-reference URLs, not local video uploads. Once submitted, BytePlus jobs are retained until completion rather than deleted on cancellation.',
  run: 'Submit exactly the approved preview/revision once with a stable UUID idempotencyKey. Requires owner-enabled account/model/agent/egress permissions and budget. Queue persists across restarts. Never replace a key after an uncertain submission.',
  cancel: 'Request cancellation of your own run. Requested does not mean cancelled or refunded. A result may finish first.',
  retry_download: 'Retrieve the already-generated result of your own failed download, without regenerating or paying for a second generation.',
  remove: 'Remove an inactive video workflow node. Active or uncertain paid runs block deletion. Generated workspace files are preserved.',
};
